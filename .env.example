# gitMaster-init v1.0.0 Environment Configuration
# Copy this file to .env and fill in your credentials

# ===== REQUIRED SETTINGS =====
# Your GitHub Personal Access Token (required scopes: repo, user:follow)
# You can use either GITHUB_TOKEN or GITHUB_PERSONAL_ACCESS_TOKEN
GITHUB_TOKEN=your_github_personal_access_token_here
# GITHUB_PERSONAL_ACCESS_TOKEN=your_github_personal_access_token_here

# Your GitHub username (auto-detected if not provided)
GITHUB_USERNAME=your_github_username

# ===== OPTIONAL SETTINGS =====
# GitHub API Configuration
GITHUB_API_BASE_URL=https://api.github.com
REQUEST_TIMEOUT=30
MAX_RETRIES=3

# Logging Configuration
LOG_LEVEL=INFO

# ===== HOW TO GET A GITHUB TOKEN =====
# 1. Go to https://github.com/settings/tokens
# 2. Click "Generate new token" -> "Generate new token (classic)"
# 3. Give it a name like "gitMaster-init"
# 4. Select these scopes:
#    - repo (Full control of private repositories)
#    - user:follow (Follow and unfollow users)
# 5. Click "Generate token"
# 6. Copy the token and paste it above (keep it secret!)

# ===== SECURITY NOTES =====
# - Never commit your .env file to version control
# - Keep your token secure and don't share it
# - Regenerate tokens periodically for security
# - Use tokens with minimal required scopes